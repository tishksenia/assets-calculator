import { AccountsList } from 'entities/AccountCard';
import { Container } from 'widgets';

export const Calculator = () => {
    return (
        <Container>
            <AccountsList />
        </Container>
    );
};
